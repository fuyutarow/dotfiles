# Evolution and verification — C5

## Compatibility table

For each subcommand, argument, flag, override, and machine route, state the promise.

The promise is stable, extensible, deprecated, or explicitly absent.

A parsed machine route names its versioning and unknown-field/record behavior.

Release identifier grammar, comparator, and range meaning belong to `designing-version-schemes`.

## Receipt lookup

| Receipt | Retain |
|---|---|
| Positive | exact command/input, stdout, stderr, exit, and contract row it proves |
| Negative | rejected command/input, stdout, stderr, exit, and expected failure class |
| Inventory relay | exact local locus for an existing command/mode/effect/outcome row |

The structural checker confirms only declared fields and tables.

It cannot prove semantic design, target behavior, source truth, or receipt observation.

Final acceptance compares the named receipt to the frozen contract.

CLI-013 supports named compatibility surfaces and Git's stable machine-route exemplar.

CLI-014 leaves universal flag/environment/file/remote precedence unresolved.

Get authority and effective-declaration policy from `governing-configuration-systems` first.

Then specify the CLI's override and reporting surface.

Grades and source limitations are in the forge ledger.

