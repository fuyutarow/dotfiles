[user]
	email = fytro10@gmail.com
	name = fytroo

[url "github:"]
	InsteadOf = https://github.com/
	InsteadOf = git@github.com:

[alias]
  ffs = flow feature start
  g = log --graph --oneline --decorate=full
  s = status
  cm = commit -m
  amend = commit --amend 
  ss = status -s
  co = checkout
  cb = checkout -b
  b = branch

  dc = git diff --cached

