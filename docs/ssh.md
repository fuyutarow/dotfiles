# SSH

```sh
ssh-keygen
```

- `ip`
- `ifconfig`
- `ping`
- `traceroute`
- `netstat`

```sh
ssh-copy-id
```

```sh
ssh -vT git@github.com
```

server
```sh
chmod 600 ~/.ssh/authorized_keys 
```

client
```sh
chmod 700 ~/.ssh
```
