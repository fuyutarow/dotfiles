# Forge verification ledger

## 2026-08-03 — initial v2 forge

| Gate / lens | Artifact or command | Result | Limitation |
|---|---|---|---|
| F1 operationality | `SKILL.md` LAW, gates, deny-list, templates | `bun agents/skills/forging-skills/scripts/skill-check.ts agents/skills/supervising-research-programmes` exited 0, no FAIL/WARN | semantic usefulness needs independent audit |
| F2 placement | signed `research-control-plane-v2` function map + reciprocal skill-name cuts | declared | sibling files are forged in parallel; re-check after merge |
| F3 trigger surface | `tests/triggers.md` | 5 FIRE / 5 NO-FIRE rows | desk-check, not guaranteed auto-trigger |
| Structural floor | `bun agents/skills/forging-skills/scripts/skill-check.ts agents/skills/supervising-research-programmes` | PASS, exit 0, no FAIL | checker is not a semantic verifier |
| Prose debt | same command | PASS, exit 0, no WARN | re-check after any SKILL.md edit |

No waiver recorded at creation. The author self-check is not independent verification.

## 2026-08-03 — acceptance repair

- Clarified executor ownership of `RUN_RECEIPT`; Director joins locator/digest and interprets it.
- Added fail-closed GENESIS versus REHYDRATE paths and mandatory `PROBLEM_LANDSCAPE` ownership.
- Added programme-only `reopen` enactment from a typed `SECTION_REOPEN_REQUEST`.
- Replaced the signal intake with the required `research-section-signal/v2` superset.

## 2026-08-03 — semantic migration repair

- Added `programme-design.md` as the sole programme-altitude home for legacy creative research design.
- Added exact declassified `AUDIT_RECOMMENDATION` intake; raw audit remains forbidden visibility.
- Replaced `SECTION_SIGNAL` with the frozen wire body for cross-skill byte identity.

## 2026-08-03 — progress lease repair

- Added event-woken lease constraints to `OPEN_ISSUE`/`SECTION_MANDATE`; mandate admission rejects
  no-action leases.
- Made CONTROL-versus-science accounting explicit; proposal-only time cannot report progress.
- Preserved raw-content quarantine while accepting only declassified denominator fields and terminal
  scientific classes. V2/V3 verification remains promotion-only and timeouts remain pending.

## 2026-08-03 — learner-role acceptance repair

- Corrected the receipt seam: executor authors receipt; `section-learner` authors the interpretation
  proposal; Director only joins, accepts/rejects, and commits section state.
- Re-ran the programme skill structural/prose floor successfully after the progress-lease reforge.

## 2026-08-03 — streaming SEARCH/LEARN North Star

- Frozen source: `STREAMING-SEARCH-LEARN-CONTRACT.md`, SHA-256
  `ab1492eb2da873b3a2ed7cfcb409acb298a3f8b072ff9326d9cd246265e7d4d2`.
- Programme semantics now treat `learnPerHour` as primary and `searchPerHour` as leading, subject
  to intent/evidence/role/relevance/safety/resource integrity. Control counts remain excluded.
- Many WIP=1 sections may stream concurrently. The cold Supervisor sees bounded aggregate signal
  windows only and never enters the reversible per-candidate hot path.
- Trigger desk-check: 9 FIRE / 11 near-miss NO-FIRE rows; no live trigger/model eval was run.
- Structural/prose floor exited 0 with no output. Cross-skill signal `cmp` exited 0.

## 2026-08-04 — aggregate transfer-propagation boundary

- Frozen source: `CROSS-SECTION-LEARNING-BUS-AMENDMENT.md`, SHA-256
  `11e83b8630f3e519e2a4c82d35d3b0a59459196ac214eb6567d793e3df02dc49`.
- The Supervisor remains outside packet subscription, routing, delivery, acknowledgement,
  interpretation, recipient admission, and local commit. It receives no transfer body.
- Programme intake is limited to bounded propagation counts/latency, replay and visibility failures,
  topic coverage gaps, conflict alarms, and an explicit SEARCH/LEARN exclusion assertion through
  byte-identical `SECTION_SIGNAL` assets.
- Trigger desk-check: 10 FIRE / 13 near-miss NO-FIRE rows. No live trigger/model evaluation ran.
- Structural/prose floor exited 0 with no output. Cross-skill signal `cmp` passed at SHA-256
  `ecbd816b12e5fe7dca8f3078820603b134e7d65116c12d9deaa60e7c359026f2`.
- No provider, repository workflow, external engine, broker runtime, or scientific run was activated.

## 2026-08-04 — live-session goal, topology, and denominator failure repair

- Live failures showed that an unsigner-owned goal could be silently rewritten, spare capacity could
  bypass a cheap upstream invalidator, and utilization/sweep activity could impersonate progress.
- Added signer-only `GOAL-CONSTITUTION`, typed prior versus evidence, exact goal lineage in snapshot,
  issue, and mandate, and receipt-linked dominance releases. Axis changes now require Goal amendment.
- The North Star now requires goal alignment, current grounding, nonduplication, minimal discrimination,
  and measurement validity. `FAIL|UNKNOWN` measurement routes to instrumentation repair; utilization is
  diagnostic only. Added aggregate grounding/duplicate/invalid-measurement/stale-work/scale-release metrics.
- Verification pending this reforge's build-order and structural/prose floor commands; no model verifier,
  link task, runtime, or scientific run was used.

## 2026-08-04 — goal/topology repair verification

- Build-order existence check: PASS; all indexed references, templates, trigger set, and ledger exist.
- `bun agents/skills/forging-skills/scripts/skill-check.ts agents/skills/supervising-research-programmes`:
  PASS, exit 0, with no FAIL or WARN output.
- `git diff --check -- agents/skills/supervising-research-programmes` passed. No PROSE-DEBT waiver is used.

## 2026-08-04 — authority-order regression repair

- Removed the impossible mandate-time grounding prerequisite. Mandates now require current Goal lineage,
  evidence interface, action path, and Programme-level eligibility release; grounding follows mandate.
- `PROGRAMME_DECISION` now exclusively releases portfolio-level dominance and issue eligibility. It does
  not release a Section's local scale; the trigger desk-check adds the corresponding regression rows.
- Build-order existence check, `skill-check.ts`, and scoped `git diff --check`: PASS with no FAIL/WARN.

## 2026-08-04 — v2 authority-chain alignment

- Mandate semantics now state `Mandate -> Charter -> Grounding -> genesis/search/admission`; no mandate-time
  grounding requirement was reintroduced. Programme issue publication points candidate genesis to that chain.
- Verification pending this reforge's floor and build-order commands.

- Build-order existence check, `skill-check.ts`, and scoped `git diff --check`: PASS with no FAIL/WARN.
