# Forge verification ledger

## 2026-07-30 — hard-gate and OPEN-pass-through reforge

The functional boundary now admits only one selected tree whose untested load-bearing result guards
expensive or irreversible downstream exposure. Cheap deterministic reversible probes route to a
domain/plain executor. `FRAME-BREAK` uses the full shared enum, and `OPEN` remains an external
provenance-bearing residual until `directing-research` classifies it.

Independent cross-skill audit: PASS-to-freeze. The core SBS/DR/FNT regression suites passed 46/46;
Codex `quick_validate.py` passed; `skill-check.ts` exited 0.

**HISTORICAL PROSE-DEBT receipt (2026-07-30).** The pre-transfer version measured 35 prose sentences
over 120 characters, with no version-block or long-table-cell warning. The active waiver is below.

## 2026-08-02 — target-side transfer guard

The transfer owner supplies a selected candidate or `MAPPING-BREAK`; this Skill owns only the selected
candidate's target-side cheapest discriminating test and its confidence update. The candidate begins
`target-side evidence: UNTESTED`. Donor success, surface similarity, and agent endorsement are
explicitly non-evidence for the target. Every target-side transfer probe returns a `TARGET RESULT` with
the frozen transfer-bundle binding, candidate ID, observation, prewritten threshold, and locus to
`directing-research`. A negative target result does not create or reclassify a `MAPPING-BREAK`. If it
calls correspondence into question, Loop requests a new mapping assessment from
`forging-novel-theses`; only that Skill may emit a break, and `directing-research` updates the
disposition. This is a constructed house boundary, not a claim that the artifact schema or cited
analogy literature proves the policy.

F3 artifact added: `tests/triggers.md` carries six fires, seven no-fires, and two ordered co-fire
routes. This Skill owns no target-result validator; the downstream Director regression binds exact
bundle bytes, rejects donor IDs/results, same-source alternate anchors, vacuous or contradictory
thresholds, negated break preservation, and post-section field borrowing. `ADOPT` requires `PASS` plus
no unresolved mapping-assessment request.

**PROSE-DEBT waiver (2026-08-02).** `skill-check.ts` reports 37 long-sentence WARNs after the
target-result contract was added. A prose-only pass must reduce them without weakening R1–R3,
target-evidence ownership, or the frozen handoff. No validator, trigger, or boundary failure is waived.

## 2026-08-02 — prose-only debt closure

Classification: prose-only. This pass split long sentences and list-shaped clauses without changing
the function map, trigger surface, artifact contracts, owners, or stop rule. It retained:

- ONE selected tree and the expensive, irreversible, untested load-bearing hard gate;
- Map / Loop / Leap and R1–R3;
- the prewritten threshold and outcome→next-action table;
- the cheap deterministic reversible executor cut;
- `TARGET RESULT`, `FRAME-BREAK`, and `MAPPING-BREAK` ownership;
- the `directing-research`, `raising-resolution`, `forging-novel-theses`,
  `surfacing-blind-spots`, multi-agent harness, and plain-executor routes;
- the rule to stop when fatal risks on the load-bearing node are retired.

The stage-1 description was not edited. Before and after it measured 991 characters with SHA-256
`7ac94d89feab5bc3c368837c96d27392b4060a00338b7c545f38b0c83bbd9b33`.

```text
before: WARN acting-on-hypotheses: 37 prose sentences >120 chars
after:  <silent: FAIL=0 WARN=0>
independent sentence count after: prose_sentences_over_120=0
```

Verification:

- Codex `quick_validate.py` -> `Skill is valid!`.
- `skill-check.ts` -> silent, `FAIL=0 WARN=0`.
- Local AOH test inventory contains no `*.test.ts`; it contains only the trigger and forge ledgers.
- The sole repository Bun test referencing `acting-on-hypotheses`,
  `directing-research/tests/research-check.test.ts`, passed 30/30 with 124 assertions.
- Atomic reference/test existence check -> exit 0 with no missing artifact.
- Scoped `git diff --check` -> exit 0.
- Frozen base remained `97ec05e84f77068144ecbaca793cf6b3a9a22a12`.

The historical 35-WARN receipt and 37-WARN waiver above remain intact as prior-state evidence.
The 37-WARN waiver is now superseded; no active prose-debt waiver remains. Structural and regression
checks do not establish the quality of a real-world hypothesis, signal, threshold, or commitment.

## 2026-08-02 — stage-1 budget compaction

Classification: description-only compaction. The stage-1 text shrank from 991 to 950 Unicode characters.
No trigger, owner, artifact, or stop rule changed.

The description still owns one selected expensive or hard-to-reverse tree, its prewritten threshold,
outcome table, and `Commit / Pivot / Kill`. It still routes cheap deterministic reversible probes to
the domain/plain executor. Target-side evidence remains required, and `MAPPING-BREAK` remains outside
this skill's authority.

The all-ten lexical contract passed the 960-character budget and the A/B, D/A, and cheap-probe cuts.
Codex `quick_validate.py` returned `Skill is valid!`. House `skill-check.ts` was silent:
`FAIL=0 WARN=0`. The directing-research suite passed 81 tests and 254 assertions.
