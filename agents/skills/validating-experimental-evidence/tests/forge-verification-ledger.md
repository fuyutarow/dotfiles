# Forge verification ledger — validating experimental evidence

## Function, existence, and ownership

```text
raw experimental receipt + effective benchmark contract
  -- validate evidence --> EVIDENCE DISPOSITION
  --> scoped numerical claim or quarantined raw observation
```

The reusable ownership void was exposed by the 2026-09-25 Firefly episode:
`orchestrating-agents` carried generic P8 footing and P9 confound semantics even
though it is a control-plane skill. It could schedule and join receipts, but a
signed map was required to fire it for a benchmark claim. Neither
`acting-on-hypotheses` (selected future bet), `directing-research-sections`
(one granted section's intent/commit), nor `practicing-tiger-style` (consequential
architecture risk) owns generic measurement validity across all experiments.
This skill takes only that semantic transition. OA's P8/P9 detailed homes are retired in
the same commit; it retains P7 resource admission and P10 intermediate reuse.

## Bounded postmortem — source is a relayed, still-live episode

The sources are two user-supplied attachments dated 2026-09-25:
`A1=6fa39883-8b35-4eb4-8718-0b143753ec4e` and
`A2=df42fc48-7fb1-4291-8187-efd90d8bf199`. These are a still-live episode,
not a frozen terminal research audit. Line numbers below refer to A1 unless
prefixed `A2:`. Do not infer that a planned check later passed. A read-only look at the live
firedancer checkout found no root `AGENTS.md`/`CLAUDE.md` and found commit
`91a4a35fa` adding `Arena.stream`/`Arena.contract` after the attachment; no
runtime test of that commit was performed in this forge.

| Observed process failure | Source locus | Corrective owner / artifact |
|---|---|---|
| Registered Arena was bypassed by direct stream calls; parameters diverged and leaderboard binding was null. The same episode reported a manually shortened SimpleLogic run. | attachment 738–756 | EV0/EV1 canonical target launch and executed-input attestation. Polysearch/Arena must enforce; skill prose cannot. |
| A very high electricity score contradicted a reported ceiling. A1 had only a leakage suspicion; A2 reports that the score stayed 1.0 after label permutation. | 633–680; A2:544–549 | EV2 rejects this run as contaminated under the reported null protocol. The precise leak path remains unknown; this forge did not inspect its raw control. |
| Tape-era leader was called a regression, then an ineligible oracle, then a transferable local mechanism; the old benchmark contract and concept eligibility were not classified before the claim. | 177–238, 702–736 | EV4 separates old artifact eligibility, mechanism transfer, replacement obligation, and current matched result. |
| A top-K experiment also changed family size 528→4,410, yet its result was used to reject a load hypothesis. | 533–537 | EV3 confound table; the run does not isolate top-K's effect. |
| A worker used an old worktree and incorrectly reported that a newer revision had not landed. Concurrent imported-file edits disrupted other runs. | 421–426, 381–400, 643–644 | EV1 observed code identity; OA dispatch read/write admission and `driving-git` worktree protocol. |
| Model won relative to a weak Transformer while missing a trivial baseline; a forward-only SSM was discussed as a learner comparison. | 408–425, 381–400 | EV3 baseline and training-protocol footing. |
| A2 labelled a telescoping six-term loss identity as an explanatory theory and listed a conditional ordering theorem beside empirical results. | A2:785–885 | EV3 preserves formal status, assumptions, and measured-variable mapping; proof validity belongs to `proving-theorems`. Independent causal terms require isolated tests. |
| A2 found current implementations using labels inside the block or reading internal votes at egress despite a stated concept forbidding both. | A2:893–907 | EV4 checks current as well as historical concept eligibility before a recovery or concept-achievement claim. |

The later report presents a matched ideal exact-key result below which checker8's
current block fell (A2:546–548). It supports a defect-relative claim under that
reported protocol, but the defect's location remains open. Khop's 70% abstention
localizes a loss; its cause and remedy remain open (A2:370–375, 688–690).
The ideal multi-byte key result supports a representational difference, not yet
the claim that the current block can learn/select that key (A2:910–941).
The conditional theorem and proposed egress remedy remain unverified in this forge.

## Provenance grades and calibration

| Rule family | Grade | Source / limit |
|---|---|---|
| Leakage means using information unavailable at prediction time | author-confirmed | [scikit-learn common pitfalls](https://scikit-learn.org/1.8/common_pitfalls.html), consulted 2026-09-25. Its examples are offline; the online invariant is skill-supplied. |
| Permuted targets generate a null under an independence hypothesis | author-confirmed | [scikit-learn permutation_test_score](https://scikit-learn.org/stable/modules/generated/sklearn.model_selection.permutation_test_score.html), consulted 2026-09-25. It does not prescribe a universal majority threshold. |
| Progressive validation predicts before learning the revealed label | author-confirmed | [River multiclass example](https://riverml.xyz/0.14.0/introduction/getting-started/multiclass-classification/), consulted 2026-09-25. Applicability is online test-then-train only. |
| Arena bypass, numerical outcomes, label-null report, claim reversals, concurrent interference | user-supplied third-party report | A1/A2 loci above; no raw numerical rerun or proof kernel receipt inspected here. |
| EV0–EV4 gates, exact perturbation, status policy | skill-supplied | General operating contract derived from failure classes; polysearch owns executable schema. |

The source actor and the model share the dominant error: a plausible number is
promoted before the effective contract, leakage controls, and scope are checked.
The skill therefore puts quarantine and allowed-claim status in its LAW and
requires target-run receipts rather than an agent's verdict.

The user's collection mission is maximizing knowledge discovery throughput from
Experimental and Formal Methods. An evidence disposition matters only when it changes
a decision or prevents a false learning commit. The cheapest discriminating check
precedes a full audit; an invalid instrument returns one named repair. Reports,
ticket counts, and even a telescoping identity are not learning by themselves.

## F2 sibling cuts and F4 standing

One claim has one canonical `EVIDENCE DISPOSITION` in its polysearch finding.
DRS owns intent, receipt, and learning;
OA owns dispatch and resources; Tiger owns the high-consequence design ledger;
type-contracts owns invariant placement; Git owns worktree operations; configuration
governance owns a registry file's authority. This skill owns what a raw score
permits a reader to assert. The reciprocal pointers and collection listing check
must be verified after the migration. A separate skill is justified only because
it replaces OA's misplaced P8/P9 material; the collection does not gain a second
semantic home for those rules.

## F3 verification record

- The built-in quick validator passed for all eight touched skills.
- Target `skill-check.ts` for this new skill exited 0 with **0 FAIL / 0 prose WARN**.
- `mise run lint:skills-floor` exited 0 at **73 skills / 64,528 listing characters**,
  below the unchanged 65,242 ratchet. Shortening `forging-skills` and
  `commanding-research-fleets` descriptions paid for the new member.
- `git diff --check` passed. `mise run link:skills` passed. Both the Codex and
  Claude links for this new skill resolve to its dotfiles source directory.
- The fire/no-fire set contains eight positive, eight near-miss negative, and
  three ordered co-fire cases. Desk-check only; installed auto-trigger success
  and downstream scientific throughput were not measured.
- Two read-only Terra adversarial audits found a pre-run EV0 bypass, bare V0
  collision, current-concept omission, a stale OA P10 gate, and a shadow Bun
  validator. The final files resolve those findings. Polysearch/Arena target
  refusal and schema changes are **not implemented by this skill**; missing
  target enforcement remains `INSTRUMENT_REPAIR`, not a PASS.

Scoped Git commit and push receipts are recorded after they occur; neither is
claimed here in advance.

## 2026-09-25 correction — remove the shadow validator

The editor initially added a Bun `evidence-check.ts`, JSON example, and skill-local tests.
The user identified the ownership error: polysearch is the sole executable schema and
promotion-gate owner. Those three files were deleted before shipping. This skill
now owns semantic predicates only and cites canonical polysearch hypothesis/run/finding
records. Any missing field or refusal belongs to polysearch/Arena implementation,
not a parallel validator inside dotfiles. The earlier ten Bun test passes were for
the withdrawn code and are **not** acceptance evidence for the final skill.
