# dotfiles

### setup
```shell
bashrc > ~/.bashrc
vimrc > ~/.vimrc
keyhac.py > keyhac/config.py
```

##### vim plugins install
```
mkdir -p ~/.vim/bundle
git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/Vundle.vim
```

